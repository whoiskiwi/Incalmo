# Environment & secrets
.env
*.key

# Range SSH keys (throwaway; generated by equifax/gen-keys.sh, never committed)
equifax/webserver/ssh/id_rsa
equifax/webserver/ssh/id_rsa.pub
equifax/database/authorized_keys
equifax/database/id_rsa.pub

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# uv
# (uv.lock is usually committed; not locking this project for now)

# Runtime output (attack logs generated per timestamp)
output/*
!output/.gitkeep

# Editor / OS
.vscode/
.idea/
.DS_Store

# Local private docs & tool config (not committed)
# Note: a # comment must be on its own line, not after a filename, or the rule breaks
# Personal replication plan / roadmap
PLAN.md
# Personal self-study code map
CODE_GUIDE.md
# Claude Code local config
.claude/
# Stage progress reports (personal notes)
reports/
# Official files kept for local reference only
reference/
